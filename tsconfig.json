{
  "compilerOptions": {
    /* Versión de JavaScript generada */
    "target": "es2020",

    /* Sistema de módulos usado por Node.js */
    "module": "commonjs",

    /* Carpeta raíz de tu código fuente */
    "rootDir": "src",

    /* Carpeta de salida de los archivos compilados */
    "outDir": "build",

    /* Habilita todas las comprobaciones estrictas */
    "strict": true,

    /* Facilita la importación de CommonJS */
    "esModuleInterop": true,

    /* Comprueba consistencia de mayúsculas/minúsculas en imports */
    "forceConsistentCasingInFileNames": true,

    /* Omite chequeo de declaración de librerías externas */
    "skipLibCheck": true,

    /* Permite importar JSON como módulo */
    "resolveJsonModule": true,

    /* Genera mapas de fuente para debugging */
    "sourceMap": true,

    /* Evita emisiones si hay errores */
    "noEmitOnError": true,

    /* Opciones adicionales de calidad */
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true
  },

  /* Archivos y carpetas incluidas en la compilación */
  "include": ["src"],

  /* Excluir node_modules y carpeta de build */
  "exclude": ["node_modules", "build"]
}
