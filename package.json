{
  "name": "lavanderia-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "print-server",
    "shared"
  ],
  "scripts": {
    "install:all": "pnpm install",
    "build:shared": "pnpm --filter @lavanderia/shared build",
    "build": "pnpm run build:shared && pnpm -r build",
    "dev": "pnpm -r dev"
  }
}
